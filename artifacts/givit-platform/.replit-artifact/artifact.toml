kind = "web"
previewPath = "/"
title = "GIVIT — AI Gift Intelligence"
version = "1.0.0"
id = "artifacts/givit-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24068

[services.development]
run = "pnpm --filter @workspace/givit-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/givit-platform", "run", "build" ]
publicDir = "artifacts/givit-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24068"
BASE_PATH = "/"
